
import { inBucket } from 'dummy/helpers/in-bucket';
import { module, test } from 'qunit';

module('Unit | Helper | in bucket');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inBucket([42]);
  assert.ok(result);
});

